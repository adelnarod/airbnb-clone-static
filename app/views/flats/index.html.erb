<h1>Flats</h1>

<ul>
  <% @flats.each do |flat| %>
    <%= link_to flat_path(flat['id']) do %>
      <li><%= flat['name'] %></li>
    <% end %>
  <% end %>
</ul>

<ul>
  <% @flats.each do |flat| %>
    <li>
      <%= link_to flat['name'], flat_path(flat['id']) %>
    </li>
  <% end %>
</ul>
